{
 "build": {
   "image": "node:alpine",
   "command": "npm install && npm run build && node ace build --production && node ace serve --ignore-ts-errors",
   "publicDir": "fleek",
   "baseDir": "",
   "environment": {
     "SOME_ENV": "Build finished!"
   }
 }
}