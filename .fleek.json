{
  "build": {
    "image": "node:alpine",
    "command": "npm install && node ace build --production && cd build && node server.js",
    "publicDir": "fleek",
    "baseDir": "",
    "environment": {
      "SOME_ENV": "Build finished!"
    }
  }
}
