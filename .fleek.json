{
  "build": {
    "image": "node:alpine",
    "command": "npm install && npm run build && node ace build --productions --no-assets && node ace serve",
    "publicDir": "fleek",
    "baseDir": "",
    "environment": {
      "SOME_ENV": "Build finished!"
    }
  }
}
