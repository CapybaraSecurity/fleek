{
 "build": {
   "image": "node:alpine",
   "command": "npm install && npm run build && echo $SOME_ENV && node ace serve",
   "publicDir": "build",
   "baseDir": "frontend",
   "environment": {
     "SOME_ENV": "Build finished!"
   }
 }
}