{
 "build": {
   "image": "node:alpine",
   "command": "npm install && npm run build && echo $SOME_ENV && node ace serve --ignore-ts-errors",
   "publicDir": "fleek",
   "baseDir": "",
   "environment": {
     "SOME_ENV": "Build finished!"
   }
 }
}